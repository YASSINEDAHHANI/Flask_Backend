services:
  # ============================================
  # MongoDB — users, projects, history, etc.
  # ============================================
  mongodb:
    image: mongo:7
    container_name: dxc_mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
    volumes:
      - mongodb_data:/data/db
    networks:
      - dxc_net
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # MinIO — PDF object storage
  # ============================================
  minio:
    image: minio/minio:latest
    container_name: dxc_minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    volumes:
      - minio_data:/data
    ports:
      - "127.0.0.1:9001:9001"   # console only, localhost (use SSH tunnel)
    networks:
      - dxc_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # Flask backend (uses Azure OpenAI for LLM)
  # ============================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dxc_backend
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - chroma_data:/app/chroma_db_minio
    depends_on:
      mongodb:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - dxc_net

  # ============================================
  # Frontend — React build served by nginx + reverse proxy to backend
  # ============================================
  frontend:
    build:
      context: ../Chat-app
      dockerfile: Dockerfile
    container_name: dxc_frontend
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      - backend
    networks:
      - dxc_net

networks:
  dxc_net:
    driver: bridge

volumes:
  mongodb_data:
  minio_data:
  chroma_data: